# for pnpm
shamefully-hoist=true
auto-install-peers=true

# Ensure CI and collaborators use the public npm registry (avoid private CodeArtifact lockfile URLs)
registry=https://registry.npmjs.org/
always-auth=false
